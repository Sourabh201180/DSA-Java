//WAP to Impliment Bubble sort

#include<stdio.h>
#include<conio.h>
#define max 5
void main()
{
    int a[max],i,j,n,temp;
    printf("Enter the no. of elements u want in an array : ");
    scanf("%d",&n);
    printf("\nEnter the Elements of array :\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        {
            printf("%d",a[i]);
            for(i=0;i<n-1;i++)
            {
                
            }
        }
    }
}